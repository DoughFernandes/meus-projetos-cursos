/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    API_URL: 'https://raw.githubusercontent.com/DoughFernandes',
    DOMAIN_ORIGIN: 'http://localhost:8000'
  }
};

export default nextConfig;
